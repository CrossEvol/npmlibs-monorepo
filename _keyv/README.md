[keyv](https://github.com/jaredwray/keyv)
lack tests for postgres and mysql. <br/>
indeed, encounter some problems with connecting to postgres. <br/>
can not serialize the circular structure when using keyv.on() with redis. <br/>